[project]
name = "rpc-model"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.3",
    "pathlib>=1.0.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
